/* 
Modulo counter(contador)
Descricao:
	Modulo que realiza uma determinada contagem de 0 a N. A quantidade de
vezes que sera feita a contagem dependera dos valores dos parametros. 
*/


module counter(
	input wire clk;
	input wire reset;
	input wire out_counter;
);






endmodule